<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Application name", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Home", "Index", "Home")</li>
                @*This allows us to have admin specific action.*@
                @if (Model.Administrator)
                {
                    <li>@Html.ActionLink("Pickups", "ApplicationUserIndex", "ApplicationUser")</li>
                }

                @*This allows us to have admin specific action.*@
                @if (Model.User || Model.Administrator)
                {
                    <li>@Html.ActionLink("Schedule", "Schedule", "Home")</li>
                    <li>@Html.ActionLink("Exception Days", "ExceptionDayIndex", "ExceptionDay")</li>
                    <li>@Html.ActionLink("Vacation Period", "VacationPeriodIndex", "VacationPeriod")</li>
                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>